# requires-user-capability: false

# Create a repository with a file so it has a default branch
fixture-repo shared REPO
env ISSUE_TITLE=${SCRIPT_NAME}-${RANDOM_STRING}

# Target the fixture repository
env GH_REPO=$ORG/$REPO

# Create an issue in the repo
exec gh issue create --title $ISSUE_TITLE --body 'Feature Body'

# Check the issue is included in the paginated list output
exec gh issue list --limit 1000 --json title --jq='.[] | select(.title == "'$ISSUE_TITLE'") | .title'
stdout $ISSUE_TITLE
